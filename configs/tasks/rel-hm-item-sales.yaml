dataset: rel-hm
task: item-sales
task_type: regression
metric: nmae
lookback_days: 730
ts_periods: [1, 7, 30, 90, 365, 730]
node_policies:
  transactions:
    feature_families: [base, conditional, temporal, sequence, episode, context]
    context_keys: [customer_id]
    max_columns: 6
