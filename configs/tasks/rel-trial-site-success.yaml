dataset: rel-trial
task: site-success
task_type: regression
metric: nmae
lookback_days: 10000
ts_periods: [30, 90, 180, 365, 730, 1825, 3650]
node_policies:
  facilities_studies:
    feature_families: [base, temporal, sequence, episode, context]
    context_keys: [nct_id]
    max_columns: 6
  outcome_analyses:
    feature_families: [base, conditional, temporal, sequence, episode, context]
    context_keys: [nct_id, outcome_id]
    max_columns: 6
  reported_event_totals:
    feature_families: [base, conditional, temporal, sequence, episode, context]
    context_keys: [nct_id]
    max_columns: 6
