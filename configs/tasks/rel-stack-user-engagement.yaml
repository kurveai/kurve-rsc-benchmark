dataset: rel-stack
task: user-engagement
task_type: classification
metric: auroc
entity_source_column: Id
lookback_days: 3650
ts_periods: [7, 30, 90, 180, 365, 730, 1825, 3650]
node_policies:
  posts:
    feature_families: [base, semantic, conditional, temporal, sequence, episode, context]
    context_keys: [OwnerUserId]
    max_columns: 4
  postHistory:
    feature_families: [base, semantic, conditional, temporal, sequence, episode]
    max_columns: 4
  comments:
    feature_families: [base, semantic, conditional, temporal, sequence, episode]
    max_columns: 4
